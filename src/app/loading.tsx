export default function Loading() {
  return (
    <main className="app-state-page" aria-live="polite">
      <section>
        <div className="app-state-mark" />
        <p className="eyebrow">MedCompass</p>
        <h1>Opening your study workspace.</h1>
        <p>Loading your private sources, notes, cards, planner, and practice history.</p>
      </section>
    </main>
  );
}
